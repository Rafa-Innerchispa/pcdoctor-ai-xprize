# Gemini API usage

Required artifact set:

- redacted `/v1/gemini/verify` response;
- matching Cloud Logging entry;
- model and request reference;
- Cloud Run revision and project;
- UTC capture time;
- reproduction instructions.

Never include the admin key, access token, credential file, or private prompt
content.
